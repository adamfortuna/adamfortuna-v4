---
title: Galleries
---

= partial 'shared/title' do
  %h1.title--header Galleries
  %p.title--description Internal page for listing galleries

= partial 'shared/content' do
  %p Galleries from all photo posts.
  %ul
    - Dir[data.gallery.galleries].each do |path|
      - gallery = Gallery::Gallery.new(path)
      %li
        %a{ href: "/galleries/#{gallery.permalink}"}= gallery.name
        %span= "(#{gallery.photos_count})"
